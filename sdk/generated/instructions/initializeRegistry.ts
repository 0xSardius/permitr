/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { PERMITR_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_REGISTRY_DISCRIMINATOR = new Uint8Array([
  189, 181, 20, 17, 174, 57, 249, 59,
]);

export function getInitializeRegistryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_REGISTRY_DISCRIMINATOR,
  );
}

export type InitializeRegistryInstruction<
  TProgram extends string = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeRegistryInstructionData = {
  discriminator: ReadonlyUint8Array;
  version: number;
};

export type InitializeRegistryInstructionDataArgs = { version: number };

export function getInitializeRegistryInstructionDataEncoder(): FixedSizeEncoder<InitializeRegistryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["version", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_REGISTRY_DISCRIMINATOR }),
  );
}

export function getInitializeRegistryInstructionDataDecoder(): FixedSizeDecoder<InitializeRegistryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["version", getU32Decoder()],
  ]);
}

export function getInitializeRegistryInstructionDataCodec(): FixedSizeCodec<
  InitializeRegistryInstructionDataArgs,
  InitializeRegistryInstructionData
> {
  return combineCodec(
    getInitializeRegistryInstructionDataEncoder(),
    getInitializeRegistryInstructionDataDecoder(),
  );
}

export type InitializeRegistryAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  version: InitializeRegistryInstructionDataArgs["version"];
};

export async function getInitializeRegistryInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
>(
  input: InitializeRegistryAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeRegistryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PERMITR_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRegistryInstructionDataEncoder().encode(
      args as InitializeRegistryInstructionDataArgs,
    ),
    programAddress,
  } as InitializeRegistryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type InitializeRegistryInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  version: InitializeRegistryInstructionDataArgs["version"];
};

export function getInitializeRegistryInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
>(
  input: InitializeRegistryInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeRegistryInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PERMITR_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRegistryInstructionDataEncoder().encode(
      args as InitializeRegistryInstructionDataArgs,
    ),
    programAddress,
  } as InitializeRegistryInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeRegistryInstruction<
  TProgram extends string = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeRegistryInstructionData;
};

export function parseInitializeRegistryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeRegistryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeRegistryInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
