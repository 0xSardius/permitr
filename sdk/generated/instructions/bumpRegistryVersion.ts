/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { PERMITR_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const BUMP_REGISTRY_VERSION_DISCRIMINATOR = new Uint8Array([
  111, 139, 224, 163, 1, 54, 50, 70,
]);

export function getBumpRegistryVersionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUMP_REGISTRY_VERSION_DISCRIMINATOR,
  );
}

export type BumpRegistryVersionInstruction<
  TProgram extends string = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type BumpRegistryVersionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type BumpRegistryVersionInstructionDataArgs = {};

export function getBumpRegistryVersionInstructionDataEncoder(): FixedSizeEncoder<BumpRegistryVersionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: BUMP_REGISTRY_VERSION_DISCRIMINATOR,
    }),
  );
}

export function getBumpRegistryVersionInstructionDataDecoder(): FixedSizeDecoder<BumpRegistryVersionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getBumpRegistryVersionInstructionDataCodec(): FixedSizeCodec<
  BumpRegistryVersionInstructionDataArgs,
  BumpRegistryVersionInstructionData
> {
  return combineCodec(
    getBumpRegistryVersionInstructionDataEncoder(),
    getBumpRegistryVersionInstructionDataDecoder(),
  );
}

export type BumpRegistryVersionAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
};

export async function getBumpRegistryVersionInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
>(
  input: BumpRegistryVersionAsyncInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BumpRegistryVersionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PERMITR_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
    ],
    data: getBumpRegistryVersionInstructionDataEncoder().encode({}),
    programAddress,
  } as BumpRegistryVersionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type BumpRegistryVersionInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
};

export function getBumpRegistryVersionInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
>(
  input: BumpRegistryVersionInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): BumpRegistryVersionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PERMITR_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
    ],
    data: getBumpRegistryVersionInstructionDataEncoder().encode({}),
    programAddress,
  } as BumpRegistryVersionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type ParsedBumpRegistryVersionInstruction<
  TProgram extends string = typeof PERMITR_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: BumpRegistryVersionInstructionData;
};

export function parseBumpRegistryVersionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBumpRegistryVersionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), config: getNextAccount() },
    data: getBumpRegistryVersionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
